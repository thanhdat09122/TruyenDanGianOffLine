<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mypc.truyenoffline">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_book_home"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_book_home"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.SpashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.ListStoryActivity" />
        <activity android:name=".activity.ContentStoryActivity"></activity>
    </application>

</manifest>